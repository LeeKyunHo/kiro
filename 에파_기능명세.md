# 에파(sd_batch_generator.py) 기능 명세
> SD WebUI API 연동 캐릭터 이미지 에셋 배치 생성 파이프라인
> 외부 AI 개선 작업용 참조 문서

---

## 1. 아키텍처 개요

```
pose_database.json                    (공용 포즈/표정 프롬프트 DB — 모든 로스터 공유)
projects/{roster}/characters/*.json   (캐릭터 프리셋)
projects/{roster}/references/{prefix}.*  (IP-Adapter 참조 이미지)
        ↓
sd_batch_generator.py                 (메인 엔진)
        ↓
SD WebUI API (/sdapi/v1/txt2img)
        ↓
projects/{roster}/assets/{prefix}/{prefix}_{코드}.webp
```

**핵심 설계 원칙**: 프롬프트 데이터는 JSON에, 로직은 Python에.
JSON 항목을 늘리면 코드 수정 없이 생성 장수가 늘어난다.

---

## 2. 로스터 시스템 (RosterPathManager)

프로젝트(캐릭터 세트) 단위 관리. `projects/` 하위 폴더 구조.

```
--roster dar    →  projects/dark_generals/
--roster oto    →  projects/oto/
--roster school →  projects/school/
--roster fth    →  projects/fth/
--roster xyz    →  projects/xyz/  (폴더만 있으면 자동 인식 — OCP 원칙)
```

각 로스터 폴더 구조:
```
projects/{roster}/
  characters/   캐릭터 JSON 파일
  references/   IP-Adapter 참조 이미지
  assets/       생성 결과 (git 제외)
```

- `pose_database.json` 은 루트에서 모든 로스터가 공유 (SSOT)
- 존재하지 않는 로스터 지정 시 사용 가능한 목록 안내 후 종료 코드 1

---

## 3. CLI 전체 옵션

### 기본 생성
| 옵션 | 기본값 | 설명 |
|---|---|---|
| `--roster / -r` | `dar` | 로스터 선택 |
| `--char NAME` | — | 캐릭터 JSON 프리셋 로드 |
| `--list` | — | 캐릭터 목록 출력 후 종료 |
| `--all-chars` | — | 로스터 전체 배치 생성 |
| `--prefix` | 필수* | 에셋 식별자 `^[A-Za-z0-9_-]{1,64}$` |
| `--char_prompt` | 필수* | 캐릭터 외형 태그 (positive 없을 때) |
| `--profile` | `female` | `_profiles` 섹션 선택 |
| `--custom_neg` | `""` | 네거티브 추가 (대체 아님) |
| `--mode` | `all` | 생성 범위 (섹션명 또는 코드 표현식) |
| `--codes` | — | 코드 직접 지정 (`--mode` 보다 우선) |

### 참조 이미지
| 옵션 | 기본값 | 설명 |
|---|---|---|
| `--ref_image` | — | 경로 직접 지정 |
| `--ref_weight` | `0.7` | 강도 0.0~2.0 |
| `--no_ref` | False | 참조 이미지 무시 |
| `--cn_module` | — | ControlNet 전처리기 수동 지정 |
| `--cn_model` | — | ControlNet 모델 수동 지정 |

### Forge 내장 기능
| 옵션 | 기본값 | 설명 |
|---|---|---|
| `--enable-freeu` | False | FreeU 활성화 |
| `--freeu-b1/b2/s1/s2` | 1.1/1.2/0.9/0.2 | FreeU 파라미터 |
| `--enable-adetailer` | False | ADetailer 얼굴 보정 |

### SDXL Lightning 가속
| 옵션 | 기본값 | 설명 |
|---|---|---|
| `--enable-lightning` | False | Lightning 2-step LoRA 가속 모드 |
| `--lightning-steps` | 2 | Lightning 스텝 수 |
| `--lightning-cfg` | 1.0 | Lightning CFG Scale |
| `--lightning-lora-name` | `sdxl_lightning_2step_lora` | Lightning LoRA 파일명 |

Lightning 활성화 시 자동 적용:
- `steps` → `--lightning-steps` 값으로 오버라이드
- `cfg_scale` → `--lightning-cfg` 값으로 오버라이드
- `sampler_name` → `DPM++ SDE`, `scheduler` → `Karras` 고정
- 프롬프트 끝에 `<lora:{name}:1.0>` 자동 주입
- FreeU와 동시 활성화 시 FreeU 자동 비활성화 (경고 출력)

### 태그 역추출
| 옵션 | 기본값 | 설명 |
|---|---|---|
| `--from_image PATH` | — | 이미지에서 태그 추출 후 종료 |
| `--interrogator` | `deepdanbooru` | `deepdanbooru` \| `clip` |

### 실행 제어
| 옵션 | 설명 |
|---|---|
| `--dry-run` | 파일·네트워크 없이 계획만 출력 |
| `--mock` | WebUI 없이 더미 이미지 실제 저장 |
| `--test` | 자체 진단 45항목 후 종료 |

---

## 4. --mode / --codes 형태

| 형태 | 예시 | 결과 |
|---|---|---|
| 프리셋 | `all` | 전체 코드 |
| 섹션명 | `emotions` | 해당 섹션만 |
| 섹션명 복수 | `emotions,poses` | 합집합 |
| 코드 열거 | `0,5,12` | 지정 코드만 |
| 코드 범위 | `10-14` | 범위 내 |
| 혼합 | `0-5,12` | 합집합 |

판별: 숫자·콤마·하이픈·공백만이면 코드 표현식, 아니면 섹션명.
`--codes`는 `--mode`보다 우선.

---

## 5. 캐릭터 JSON 스펙

### positive 방식 (권장 — 프로필 완전 대체)
```json
{
  "prefix": "rei",
  "default_mode": "female",
  "positive": "masterpiece, best quality, ..., 1girl, solo, 캐릭터 외형...",
  "negative": "1boy, male, child, loli, uncensored, no censor, thin censorship",
  "ref_weight": 0.7
}
```

### char_prompt 방식 (레거시 — 프로필과 조합)
```json
{
  "prefix": "bel",
  "profile": "male_otokonoko",
  "default_mode": "otokonoko",
  "char_prompt": "platinum blonde bob, golden eyes, military miniskirt",
  "custom_neg": "1girl, female anatomy",
  "ref_weight": 0.7
}
```

**우선순위**: CLI 명시값 > 캐릭터 JSON > 기본값

**default_mode 값**:
- `female` → `emotions,poses,h_scenes` 코드 00-75
- `otokonoko` → `emotions,poses,scenes_otokonoko` 코드 00-39, 140-170

**동적 해상도**: `"width": 1216, "height": 832` 필드로 씬별 해상도 오버라이드 가능

---

## 6. pose_database.json 스펙

```json
{
  "_schema": { "...": "메타. 파싱 제외" },
  "_profiles": {
    "female":           { "base_positive": "..., 1girl, solo", "base_negative": "..., 1boy, male" },
    "male":             { "base_positive": "..., 1boy, solo, masculine", "base_negative": "..., 1girl, female" },
    "male_otokonoko":   { "base_positive": "..., 1boy, androgynous, feminine face", "base_negative": "..., muscular, manly" }
  },
  "emotions":           { "00": "프롬프트", "01": "..." },
  "poses":              { "21": "...", "30": "..." },
  "h_scenes":           { "40": "...", "75": "..." },
  "scenes_otokonoko":   { "140": "...", "170": "..." }
}
```

**현재 번호 범위**:
- `00-20`: 감정/표정 (공용)
- `21-39`: 포즈/상호작용 (공용)
- `40-75`: H씬 여성
- `140-170`: H씬 오토코노코

**규칙**:
- **첫 태그 = 콘솔 라벨** — 구별 가능한 서술어를 맨 앞에
- 성별 태그(`1girl`/`1boy`)는 포즈에 넣지 않음 (`_profiles` 축)
- 코드는 문자열이지만 정수로 정렬됨
- `_`로 시작하는 키는 파싱 제외
- 코드 범위: 0~9999

---

## 7. 프롬프트 조립

**positive (char_prompt 방식)**:
```
{profile.base_positive}, {char_prompt}, {포즈 프롬프트}, {prefix}_{코드}
```

**positive (positive 방식)**:
```
{positive}  ← 프로필/char_prompt 완전 무시, 포즈 태그는 뒤에 추가
```

**negative**:
```
{profile.base_negative}, {custom_neg}
```

**Lightning LoRA 활성화 시**:
```
<lora:{name}:1.0>, {positive_or_assembled}
```

---

## 8. 참조 이미지 (IP-Adapter)

- **탐색 순서**: `projects/{roster}/references/{prefix}.png → .jpg → .jpeg → .webp`
- **부재**: 경고 출력 후 텍스트만으로 생성 계속 (에러 아님)
- **명시 경로 부재**: ConfigError로 즉시 중단
- **ControlNet 자동 탐지**: `/controlnet/module_list`, `/controlnet/model_list` 조회 후 `ip-adapter` 패턴 부분 매칭
- **탐지 실패**: 사용 가능 목록 전부 출력 → `--cn_module`/`--cn_model` 수동 지정 안내
- **mock에서**: ControlNet 조회 안 함. 수동 지정 시 주입 경로만 검증

---

## 9. Graceful Fallback

WebUI API가 422/500 에러 반환 시 (FreeU/ADetailer 미설치 등):
1. `[WARN]` 경고 출력
2. FreeU/ADetailer 비활성화한 순정 페이로드로 자동 재시도
3. ControlNet 유닛은 유지

404 에러 (WebUI 미실행)는 즉시 중단.

---

## 10. 실행 모드 비교

| | 기본 | --mock | --dry-run | --test |
|---|---|---|---|---|
| API 호출 | ✅ | ❌ | ❌ | ❌ |
| 파일 저장 | ✅ | ✅ (더미) | ❌ | ❌ |
| 참조 이미지 인코딩 | ✅ | ✅ | ❌ | ✅ (임시) |
| ControlNet 조회 | ✅ | ❌ | ❌ | ❌ |
| VRAM 조회 | ✅ | ❌ | ❌ | ❌ |
| 마크다운 대상 | 실존 파일 | 실존 파일 | 전체 계획 | 없음 |

---

## 11. 저장 및 재개

- **경로**: `projects/{roster}/assets/{prefix}/{prefix}_{코드}.webp`
- **패딩**: 전체 DB 최대 코드 자릿수 기준, 최소 2자리
- **원자적 저장**: `.part` 임시 파일 → `os.replace()` 교체 (중단 시 반쪽 파일 방지)
- **재개**: 기존 파일 있으면 자동 건너뜀. 삭제 후 재실행하면 해당 코드만 재생성

---

## 12. 출력 형식

```
[ROSTER] 'school' -> projects/school/characters
[CHAR]   'rei' 프리셋 로드
[LIGHTNING] Mode Enabled: 4 Steps | CFG 1.5 | Sampler: DPM++ SDE Karras | LoRA: <lora:sdxl_lightning_2step_lora:1.0>
[REF]    rei.webp (832x1216) weight 0.7
[CN]     ip-adapter_clip_sdxl / ip-adapter_xl [...] (auto)
[SAMPLER] 'DPM++ 2M SDE Karras' 감지됨
[작업 시작] 캐릭터: rei | 프로필: 전용 | 모드: all | 폭: 3

  [000] 생성 중... 완료 (16.3초) -> rei_000.webp
  [001] 이미 존재 (건너뜀) -> rei_001.webp

[작업 완료] 성공 1 / 건너뜀 1 / 실패 0
[측정] 1장 / 총 16.3초 / 장당 평균 16.3초
[VRAM] 피크 5.98 / 8.00 GiB (75%)
```

**젠잇 마크다운 블록 (4섹션)**:
1. 이미지 호출 코드: `![image]({{url}}prefix/prefix_000.webp)`
2. 파일 목록
3. 상태 매핑 가이드 (섹션별 라벨 → 파일명)
4. 상태창 템플릿

`{{url}}`은 젠잇이 치환하는 리터럴 — 그대로 출력.

---

## 13. 자체 진단 (--test) 45항목

- **T1~T7**: JSON 파일/문법/섹션 타입/비정수 키/빈 프롬프트/중복 코드/유효 엔트리
- **T8~T17**: 정수 정렬/code_width/코드 파싱/파일명 조립/마크다운/`{{url}}`/prefix 화이트리스트/태그 정규화/충돌 감지
- **T18~T20**: 프로필 로드/태그 충돌/기본 프로필 female 존재
- **T21~T32**: 참조 이미지 탐색/base64/페이로드 조립/원본 불변성/ref_weight/interrogate/성별 태그 필터/모델명 매칭/시간 집계/VRAM 파싱

---

## 14. 종료 코드

| 코드 | 의미 |
|---|---|
| `0` | 정상 |
| `1` | ConfigError / WebUI 연결 끊김 / --test FAIL |
| `2` | 필수 인자 누락 (argparse) |
| `130` | Ctrl+C |

---

## 15. 생성 파라미터 상수

| 상수 | 현재값 | 설명 |
|---|---|---|
| `IMAGE_SIZE` | `(832, 1216)` | SDXL 세로 초상화 |
| `STEPS` | `24` | 일반 모드 스텝 수 |
| `CFG_SCALE` | `6.0` | |
| `WEBP_QUALITY` | `90` | |
| `WEBP_METHOD` | `6` | |
| `REF_WEIGHT_DEFAULT` | `0.7` | |
| `LORA_STRING` | `""` | 전역 LoRA (비어있으면 미적용) |
| `SAMPLER_CANDIDATES` | `DPM++ 2M SDE Karras`, `DPM++ 2M Karras`, `DPM++ 2M`, `Euler a` | 우선순위 순 |
| API | `http://127.0.0.1:7860` | |

---

## 16. 빠른 명령어 참조

```powershell
# 전체 생성
python -u sd_batch_generator.py --all-chars -r dar
python -u sd_batch_generator.py --all-chars -r school

# 단일 캐릭터
python -u sd_batch_generator.py --char rog -r dar

# 특정 번호
python -u sd_batch_generator.py --char rei -r school --codes "0,1,2,3"
python -u sd_batch_generator.py --char bel -r dar --codes "140-170"

# 재생성
Remove-Item "projects\dark_generals\assets\rog\rog_020.webp"
python -u sd_batch_generator.py --char rog -r dar --codes "20"

# 목록 확인
python sd_batch_generator.py --list -r school

# 진단 (WebUI 불필요)
python sd_batch_generator.py --test

# Lightning 가속
python -u sd_batch_generator.py --char rei -r school --codes "0" --enable-lightning --lightning-steps 4 --lightning-cfg 1.5

# FreeU 품질 강화
python -u sd_batch_generator.py --all-chars -r school --enable-freeu
```

---

## 17. 알려진 이슈 및 베스트 프랙티스

### 도플갱어/의상 복제 방지
- `pov` 사용으로 파트너를 뷰어로 처리
- `masculine hands`, `masculine fingers` — 신체 부위만 묘사
- `faceless male, no facial features, bald, featureless head, skin-colored head` — 모브 캐릭터
- `solo focus` 추가로 캐릭터 1인 강제

### 표정 고정 문제
- 캐릭터 JSON `positive`에 표정 태그 고정 금지 (`smile`, `expression` 등)
- 포즈 DB의 표정 태그가 덮어씌워지도록 캐릭터 JSON은 외형/의상만

### 의상 벗겨짐 연출
- 캐릭터 JSON 의상 가중치를 `1.0`으로 낮추면 포즈 태그가 쉽게 덮어씀
- h씬 포즈에 `top pushed up`, `skirt hiked`, `clothes disheveled` 태그로 유도

### BOM 문제 방지
- 스크립트 내부에서 `encoding='utf-8-sig'`로 읽음 (BOM 자동 무시)
- JSON 저장 시: `[System.IO.File]::WriteAllText($path, $content, [System.Text.UTF8Encoding]::new($false))`

### PowerShell 출력 버퍼링
- `python -u` 플래그로 실행 (unbuffered — 출력 중간 잘림 방지)
- 출력이 잘려도 실제 생성은 완료됨 (파일 확인으로 검증)

### 검열 (모자이크)
- 성기 부위만: `(black bar censor on genitals:1.5)`
- 가슴/얼굴은 모자이크 불필요

### 절정 포즈 표정
- 절정 전: `heavy-lidded pleasure eyes, flushed cheeks, soft panting lips`
- 절정 후: `ahegao, rolling eyes, tears streaming`
