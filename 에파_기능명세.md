# 에파(sd_batch_generator.py) 기능 명세
> SD WebUI API 연동 캐릭터 이미지 에셋 배치 생성 파이프라인
> 다른 AI 개선 작업용 참조 문서

---

## 1. 아키텍처 개요

```
pose_database.json          (공용 포즈/표정 프롬프트 DB)
characters_{roster}/*.json  (캐릭터 프리셋)
references/{prefix}.*       (IP-Adapter 참조 이미지)
        ↓
sd_batch_generator.py       (메인 엔진)
        ↓
SD WebUI API (/sdapi/v1/txt2img)
        ↓
generated_assets_{roster}/{prefix}/{prefix}_{코드}.webp
```

**핵심 설계 원칙**: 프롬프트 데이터는 JSON에, 로직은 Python에.
JSON 항목을 늘리면 코드 수정 없이 생성 장수가 늘어난다.

---

## 2. 로스터 시스템 (RosterPathManager)

프로젝트(캐릭터 세트) 단위 관리.

```
--roster dar  →  characters_dark_generals/  +  generated_assets_dark_generals/
--roster oto  →  characters_oto/            +  generated_assets_oto/
--roster xyz  →  characters_xyz/            (폴더만 있으면 자동 인식 - OCP 원칙)
```

- `pose_database.json`, `references/`는 루트에서 모든 로스터 공유 (SSOT)
- 존재하지 않는 로스터 지정 시 사용 가능한 목록 자동 안내 후 종료 코드 1

---

## 3. CLI 전체 옵션

### 기본 생성
| 옵션 | 기본값 | 설명 |
|---|---|---|
| `--roster / -r` | `dar` | 로스터 선택 |
| `--char NAME` | — | 캐릭터 JSON 프리셋 로드 |
| `--list` | — | 캐릭터 목록 출력 후 종료 |
| `--all-chars` | — | 로스터 전체 배치 생성 |
| `--prefix` | 필수* | 에셋 식별자 `^[A-Za-z0-9_-]{1,64}$` |
| `--char_prompt` | 필수* | 캐릭터 외형 태그 |
| `--profile` | `female` | `_profiles` 섹션 선택 |
| `--custom_neg` | `""` | 네거티브 추가 (대체 아님) |
| `--mode` | `all` | 생성 범위 |
| `--codes` | — | 코드 직접 지정 (`--mode` 보다 우선) |

### 참조 이미지
| 옵션 | 기본값 | 설명 |
|---|---|---|
| `--ref_image` | — | 경로 직접 지정 |
| `--ref_weight` | `0.7` | 강도 0.0~2.0 |
| `--no_ref` | False | 참조 이미지 무시 |
| `--cn_module` | — | ControlNet 전처리기 수동 지정 |
| `--cn_model` | — | ControlNet 모델 수동 지정 |

### 태그 역추출
| 옵션 | 기본값 | 설명 |
|---|---|---|
| `--from_image PATH` | — | 이미지에서 태그 추출 후 종료 |
| `--interrogator` | `deepdanbooru` | `deepdanbooru` \| `clip` |

### 실행 제어
| 옵션 | 설명 |
|---|---|
| `--dry-run` | 파일·네트워크 없이 계획만 출력 |
| `--mock` | WebUI 없이 더미 이미지 실제 저장 |
| `--test` | 자체 진단 46항목 후 종료 |

---

## 4. --mode 5가지 형태

| 형태 | 예시 | 결과 |
|---|---|---|
| 프리셋 | `all` | 전체 코드 |
| 섹션명 | `emotions` | 해당 섹션만 |
| 섹션명 복수 | `emotions,poses` | 합집합 |
| 코드 열거 | `0,5,12` | 지정 코드만 |
| 코드 범위 | `10-14` | 범위 내 |
| 혼합 | `0-5,12` | 합집합 |

판별 방식: 숫자·콤마·하이픈·공백만이면 코드 표현식, 아니면 섹션명.

---

## 5. 캐릭터 JSON 스펙

```json
{
  "prefix": "bel",
  "default_mode": "otokonoko",
  "positive": "캐릭터 전용 포지티브 (프로필 완전 대체)",
  "negative": "캐릭터 전용 네거티브",
  "profile": "male_otokonoko",
  "char_prompt": "외형 태그 (positive 없을 때 사용)",
  "custom_neg": "추가 네거티브",
  "ref_weight": 0.7
}
```

**우선순위**: CLI 명시값 > 캐릭터 JSON > 기본값
**default_mode**: `female` → `emotions,poses,h_scenes` / `otokonoko` → `emotions,poses,scenes_otokonoko`

---

## 6. pose_database.json 스펙

```json
{
  "_schema": { "...": "메타. 파싱 제외" },
  "_profiles": {
    "female":           { "base_positive": "...", "base_negative": "..." },
    "male":             { "base_positive": "...", "base_negative": "..." },
    "male_otokonoko":   { "base_positive": "...", "base_negative": "..." }
  },
  "emotions":           { "00": "프롬프트", "01": "..." },
  "poses":              { "21": "...", "22": "..." },
  "h_scenes":           { "40": "...", "41": "..." },
  "scenes_otokonoko":   { "140": "...", "141": "..." }
}
```

**번호 범위**:
- `00-20`: 감정/표정 (공용)
- `21-33`: 포즈/상호작용 (공용)
- `40-70`: H씬 여성
- `140-170`: H씬 오토코노코

**규칙**:
- 첫 태그 = 콘솔 라벨 (구별 가능한 서술어로)
- 성별 태그는 포즈에 넣지 않음 (`_profiles` 축에서 관리)
- 코드는 문자열이지만 정수로 정렬됨
- `_`로 시작하는 키는 파싱 제외

---

## 7. 프롬프트 조립

**포지티브** (캐릭터 JSON `positive` 없을 때):
```
{profile.base_positive}, {char_prompt}, {포즈 프롬프트}, {prefix}_{코드}
```

**포지티브** (캐릭터 JSON `positive` 있을 때):
```
{positive}  ← 프로필/char_prompt 완전 무시
```

**네거티브**:
```
{profile.base_negative}, {custom_neg}
```

---

## 8. 참조 이미지 (IP-Adapter)

- **탐색 순서**: `references/{prefix}.png → .jpg → .jpeg → .webp`
- **부재**: 경고 출력 후 텍스트만으로 생성 계속 (에러 아님)
- **명시 경로 부재**: ConfigError로 즉시 중단
- **ControlNet 자동 탐지**: `/controlnet/module_list`, `/controlnet/model_list` 조회 후 `ip-adapter` 패턴 부분 매칭
- **탐지 실패**: 사용 가능 목록 전부 출력 → `--cn_module`/`--cn_model` 수동 지정 안내
- **mock에서**: ControlNet 조회 안 함. 수동 지정 시 주입 경로만 검증 가능

---

## 9. 실행 모드 비교

| | 기본 | --mock | --dry-run | --test |
|---|---|---|---|---|
| API 호출 | ✅ | ❌ | ❌ | ❌ |
| 파일 저장 | ✅ | ✅ (더미) | ❌ | ❌ |
| 참조 이미지 인코딩 | ✅ | ✅ | ❌ | ✅ (임시파일) |
| ControlNet 조회 | ✅ | ❌ | ❌ | ❌ |
| VRAM 조회 | ✅ | ❌ | ❌ | ❌ |
| 마크다운 대상 | 실존 파일 | 실존 파일 | 전체 | 없음 |

---

## 10. 저장 및 재개

- **경로**: `generated_assets_{roster}/{prefix}/{prefix}_{코드}.webp`
- **패딩**: 전체 DB 최대 코드 자릿수 기준, 최소 2자리
- **원자적 저장**: `.part` 임시 파일 → `os.replace()` 교체
- **재개**: 기존 파일 있으면 자동 건너뜀. 삭제 후 재실행하면 해당 코드만 재생성
- **중단 후 재실행**: 끊긴 지점부터 이어서 생성

---

## 11. 출력 형식

```
[ROSTER] 'dark_generals' -> characters_dark_generals/
[CHAR]   'bel' 프리셋 로드
[PROFILE] 'female' 적용
[REF]    bel.webp (832x1216) weight 0.7
[CN]     ip-adapter_clip_sdxl / ip-adapter_xl [...] (auto)
[SAMPLER] 'DPM++ 2M Karras' 감지됨
[작업 시작] 캐릭터: bel | 프로필: female | 모드: all (20장) | 폭: 3

  [000] 생성 중... 완료 (20.3초) -> bel_000.webp
  [001] 이미 존재 (건너뜀) -> bel_001.webp

[작업 완료] 성공 19 / 건너뜀 1 / 실패 0
[측정] 19장 / 총 385.7초 / 장당 평균 20.3초
[VRAM] 피크 7.21 / 8.00 GiB (90%)
```

**젠잇 마크다운 블록 (4섹션)**:
1. 이미지 호출 코드: `![image]({{url}}prefix/prefix_000.webp)`
2. 파일 목록
3. 상태 매핑 가이드 (섹션별 라벨 → 파일명)
4. 상태창 템플릿

`{{url}}`은 젠잇이 치환하는 리터럴 — 그대로 출력.

---

## 12. 자체 진단 (--test) 46항목

- **T1~T7**: JSON 파일/문법/섹션 타입/비정수 키/빈 프롬프트/중복 코드/유효 엔트리
- **T8~T17**: 정수 정렬/code_width/코드 파싱/파일명 조립/마크다운/{{url}}/prefix 화이트리스트/태그 정규화/충돌 감지
- **T18~T20**: 프로필 로드/태그 충돌/기본 프로필
- **T21~T36**: 참조 이미지 탐색/base64/페이로드 조립/원본 불변성/ref_weight/interrogate/성별 태그 필터/모델명 매칭/시간 집계/VRAM 파싱/캐릭터 프리셋

---

## 13. 종료 코드

| 코드 | 의미 |
|---|---|
| `0` | 정상 |
| `1` | ConfigError / WebUI 연결 끊김 / --test FAIL |
| `2` | 필수 인자 누락 |
| `130` | Ctrl+C |

---

## 14. 보안 제약

- `--prefix` → `^[A-Za-z0-9_-]{1,64}$` 강제 (경로 이탈, 주입 방지)
- 탐색기 오픈 → `os.startfile()` (셸 우회)
- 코드 범위 상한 9999 (메모리 폭주 방지)

---

## 15. 생성 파라미터 상수

| 상수 | 값 |
|---|---|
| `IMAGE_SIZE` | `(832, 1216)` SDXL 세로 초상화 |
| `STEPS` | `28` |
| `CFG_SCALE` | `7` |
| `WEBP_QUALITY` | `90` |
| `WEBP_METHOD` | `6` |
| `REF_WEIGHT_DEFAULT` | `0.7` |
| `SAMPLER_CANDIDATES` | `DPM++ 2M Karras`, `DPM++ 2M`, `Euler a` |
| API | `http://127.0.0.1:7860` |

---

## 16. 사용법 빠른 참조

```powershell
# 전체 생성
python sd_batch_generator.py --all-chars -r dar
python sd_batch_generator.py --all-chars -r oto

# 단일 캐릭터
python sd_batch_generator.py --char rog -r dar

# 특정 번호
python sd_batch_generator.py --char rog -r dar --mode 20
python sd_batch_generator.py --char rog -r dar --mode 140-170
python sd_batch_generator.py --char rog -r dar --mode 0,7,24

# 재생성
Remove-Item generated_assets_dark_generals\rog\rog_020.webp
python sd_batch_generator.py --char rog -r dar --mode 20

# 목록 확인
python sd_batch_generator.py --list -r dar

# 진단
python sd_batch_generator.py --test

# 계획 확인 (WebUI 불필요)
python sd_batch_generator.py --char rog -r dar --dry-run
```

---

## 17. 알려진 이슈 및 베스트 프랙티스

### 도플갱어/의상 복제 방지
- `pov` 사용으로 파트너를 뷰어로 처리
- `masculine hands`, `masculine fingers` — 신체 부위만 묘사
- `faceless male, no facial features, bald, featureless head, skin-colored head` — 모브 캐릭터 태그
- `solo focus` 추가
- `straddling partner` → `straddling, seated position`
- `grounded, body contact` — 공중 부유 방지

### 표정 고정 문제
- 캐릭터 JSON의 `positive`에 smile/expression 태그 고정 금지
- 포즈 DB의 표정 태그가 덮어씌워지도록 캐릭터 JSON은 외형/의상만 담기

### BOM 문제
- IDE에서 직접 JSON 저장 시 UTF-8 BOM이 붙을 수 있음
- 수정은 Python으로: `json.loads(p.read_text(encoding='utf-8-sig'))`로 읽고 `encoding='utf-8'`로 저장

### 모자이크/검열
- `solid black censor bar, opaque black censor, black censorship:1.5`
- 오토코노코 하체: `black censor bar over groin:1.3` 추가

### 절정 포즈 접촉 강화
- 동작 동사 사용: `sucking`, `licking`, `gripping`
- 직접 접촉 명시: `lips pressed against`, `direct skin contact`
- `pressed against`, `body contact`, `grounded`
