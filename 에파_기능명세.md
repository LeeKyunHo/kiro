# 에파(sd_batch_generator.py) 기능 명세
> **문서 목적**: SD WebUI 연동 캐릭터 이미지 에셋 배치 생성 파이프라인의 전체 기술 및 인터페이스 규격서.  
> **대상 독자**: 외부 AI 에이전트, 스크립트 연동 자동화 도구, 파이프라인 API 분석자.  
> **다른 문서 바로가기**:  
> - 👤 사용자용 설치 및 간편 실행 치트시트 → [`사용법.txt`](file:///c:/Users/rbsgh/kiro/%EC%82%AC%EC%9A%A9%EB%B2%95.txt)  
> - 🛠️ 내부 아키텍처 및 개발자 가이드 → [`PROJECT_GUIDE.md`](file:///c:/Users/rbsgh/kiro/PROJECT_GUIDE.md)  
> - 🎨 창작자용 캐릭터 외형/포즈/Danbooru 태그 작성 규칙 → [`캐릭터_포즈_제작_규칙.txt`](file:///c:/Users/rbsgh/kiro/%EC%BA%90%EB%A6%AD%ED%84%B0_%ED%8F%AC%EC%A6%88_%EC%A0%9C%EC%9E%91_%EA%B7%9C%EC%B9%99.txt)

---

## 📑 목차 (Table of Contents)

1. [1. 아키텍처 및 모듈 개요](#1-아키텍처-및-모듈-개요)
2. [2. 로스터 시스템 (RosterPathManager)](#2-로스터-시스템-rosterpathmanager)
3. [3. CLI 전체 옵션 규격](#3-cli-전체-옵션-규격)
   - [3.1 기본 생성 및 캐릭터 제어](#31-기본-생성-및-캐릭터-제어)
   - [3.2 배경 제어 (Background System)](#32-배경-제어-background-system)
   - [3.3 참조 이미지 및 ControlNet (IP-Adapter)](#33-참조-이미지-및-controlnet-ip-adapter)
   - [3.4 Forge / WebUI 스크립트 확장](#34-forge--webui-스크립트-확장)
   - [3.5 SDXL Lightning 가속 모드](#35-sdxl-lightning-가속-모드)
   - [3.6 실행 제어 및 진단](#36-실행-제어-및-진단)
4. [4. 코드 표현식 및 모드 해석 (--mode, --codes)](#4-코드-표현식-및-모드-해석---mode---codes)
5. [5. 데이터 스펙](#5-데이터-스펙)
   - [5.1 캐릭터 JSON 스펙 (characters/{name}.json)](#51-캐릭터-json-스펙-charactersnamejson)
   - [5.2 포즈 데이터베이스 스펙 (pose_database.json)](#52-포즈-데이터베이스-스펙-pose_databasejson)
   - [5.3 프로젝트 전용 이벤트 스펙 (events.json)](#53-프로젝트-전용-이벤트-스펙-eventsjson)
   - [5.4 프로젝트 전용 배경 스펙 (background.json)](#54-프로젝트-전용-배경-스펙-backgroundjson)
6. [6. 프롬프트 조립 메커니즘 (assemble_prompt)](#6-프롬프트-조립-메커니즘-assemble_prompt)
   - [6.1 포지티브 조립 규칙 (BREAK 전진 배치)](#61-포지티브-조립-규칙-break-전진-배치)
   - [6.2 네거티브 조립 규칙](#62-네거티브-조립-규칙)
7. [7. WebUI 통신 및 페이로드 규격](#7-webui-통신-및-페이로드-규격)
   - [7.1 기본 txt2img 페이로드](#71-기본-txt2img-페이로드)
   - [7.2 ControlNet (IP-Adapter) 주입 규격](#72-controlnet-ip-adapter-주입-규격)
   - [7.3 Graceful Fallback 에러 복구](#73-graceful-fallback-에러-복구)
8. [8. 저장소 및 파일 I/O 불변식](#8-저장소-및-파일-io-불변식)
9. [9. 종료 코드 (Exit Codes)](#9-종료-코드-exit-codes)
10. [10. 자체 진단 테스트 스위트 (T1~T37e, 총 52개)](#10-자체-진단-테스트-스위트-t1t37e-총-52개)

---

## 1. 아키텍처 및 모듈 개요

```
pose_database.json (공용 포즈 DB) ──┐
projects/{roster}/events.json    ──┼─→ generator.pose_db.load_pose_db()
projects/{roster}/background.json ─┘          │
                                              ▼
projects/{roster}/characters/*.json  ──→ generator.roster.apply_character_to_args()
projects/{roster}/references/*       ──→ generator.reference.resolve_reference_image()
                                              │
                                              ▼
                                     generator.runner.execute()
                                       ├─ _setup_prompt()      포지티브/네거티브 결정
                                       ├─ _setup_reference()   참조이미지 + ControlNet
                                       └─ _setup_sampler()     샘플러 선택
                                              │
                                              ▼
                                     SD WebUI API (/sdapi/v1/txt2img)
                                              │
                                              ▼
                                     projects/{roster}/assets/{prefix}/{prefix}_{코드}.webp
```

**핵심 설계 원칙**:
- 프롬프트 데이터는 JSON에 격리 (`pose_database.json`, `events.json`, `background.json`).
- 파이프라인 로직은 Python 패키지(`generator/`)에 캡슐화.
- CLI 진입점(`sd_batch_generator.py`)은 인자 파싱 및 위임만 담당.
- `execute()`는 오케스트레이션만 담당하고 세부 결정은 `_setup_*` 헬퍼에 위임.

---

## 2. 로스터 시스템 (RosterPathManager)

작품(프로젝트)별 독립 데이터 격리 및 OCP(개방-폐쇄 원칙) 구조.

```
projects/
  {roster}/
    characters/       캐릭터 프리셋 JSON (*.json)
    references/       IP-Adapter 참조 이미지 (*.png, *.jpg, *.jpeg, *.webp)
    assets/           생성 결과 WebP 파일 (git 제외: projects/*/assets/)
    events.json       [선택] 프로젝트 전용 이벤트 포즈 DB (200번대 등 자동 병합)
    background.json   [선택] 프로젝트 감정(emotions) 씬 전용 배경 프리셋
```

- **별칭 매핑**:
  - `dar` → `projects/dark_generals/` (흑막사천왕)
  - `sea` → `projects/sea/` (바다/비치 바)
  - `oto` → `projects/oto/` (오토코노코)
  - `school` → `projects/school/` (학원물)
  - `fth` → `projects/fth/` (판타지 하렘)
- **동적 로스터 확장**: `projects/xyz/` 디렉터리가 생성되면 `--roster xyz`가 코드 수정 없이 자동 인식됨.

---

## 3. CLI 전체 옵션 규격

### 3.1 기본 생성 및 캐릭터 제어
| 옵션 | 기본값 | 설명 |
|---|---|---|
| `--roster / -r` | `dar` | 대상 로스터(프로젝트) 지정 |
| `--char NAME` | — | 단일 캐릭터 프리셋 로드 (`characters/{NAME}.json`) |
| `--list` | — | 해당 로스터의 캐릭터 목록 출력 후 종료 |
| `--all-chars` | — | 해당 로스터 내 모든 캐릭터 순차 일괄 생성 |
| `--prefix` | 필수* | 에셋 파일명 식별자 (`^[A-Za-z0-9_-]{1,64}$`) |
| `--char_prompt` | 필수* | 캐릭터 외형 태그 (캐릭터 JSON의 `positive` 없을 때) |
| `--profile` | `female` | `pose_database.json`의 `_profiles` 섹션 지정 (`female`, `male`, `male_otokonoko`) |
| `--custom_neg` | `""` | 추가 네거티브 프롬프트 |
| `--mode` | `all` | 생성 범위 지정 (섹션명 또는 코드 표현식) |
| `--codes` | — | 생성 코드 직접 지정 (`--mode`보다 최우선 적용) |

### 3.2 배경 제어 (Background System)
| 옵션 | 기본값 | 설명 |
|---|---|---|
| `--bg` | — | 감정 씬(00~20)에 강제 적용할 배경 프롬프트 (최우선순위) |
| `--bg-preset` | `default` | `background.json`에 정의된 배경 프리셋 키 선택 |

### 3.3 참조 이미지 및 ControlNet (IP-Adapter)
| 옵션 | 기본값 | 설명 |
|---|---|---|
| `--ref_image` | — | IP-Adapter 참조 이미지 파일 경로 직접 지정 |
| `--ref_weight` | `0.7` | IP-Adapter 제어 가중치 (0.0 ~ 2.0 허용) |
| `--no_ref` | False | 참조 이미지가 존재해도 강제로 무시하고 순수 텍스트 생성 |
| `--cn_module` | — | ControlNet 전처리기 모듈 수동 지정 (미지정 시 자동 탐지) |
| `--cn_model` | — | ControlNet 모델명 수동 지정 (미지정 시 자동 탐지) |

### 3.4 Forge / WebUI 스크립트 확장
| 옵션 | 기본값 | 설명 |
|---|---|---|
| `--enable-freeu` | False | FreeU 품질 개선 알고리즘 활성화 |
| `--freeu-b1` | 1.1 | FreeU 파라미터 b1 (기본 1.1) |
| `--freeu-b2` | 1.2 | FreeU 파라미터 b2 (기본 1.2) |
| `--freeu-s1` | 0.9 | FreeU 파라미터 s1 (기본 0.9) |
| `--freeu-s2` | 0.2 | FreeU 파라미터 s2 (기본 0.2) |
| `--enable-adetailer` | False | ADetailer 얼굴 보정 스크립트 활성화 (`face_yolov8n.pt`) |

### 3.5 SDXL Lightning 가속 모드
| 옵션 | 기본값 | 설명 |
|---|---|---|
| `--enable-lightning` | False | Lightning LoRA 2~4스텝 초고속 생성 모드 활성화 |
| `--lightning-steps` | 2 | Lightning 스텝 수 오버라이드 |
| `--lightning-cfg` | 1.0 | Lightning CFG Scale 오버라이드 |
| `--lightning-lora-name`| `sdxl_lightning_2step_lora` | 적용할 Lightning LoRA 파일명 |

*Lightning 활성화 시 자동 규칙:*
- `sampler_name` → `DPM++ SDE`, `scheduler` → `Karras` 자동 고정
- 프롬프트 맨 앞에 `<lora:{name}:1.0>` 자동 주입
- FreeU 활성화 상태면 상충 방지를 위해 FreeU 자동 비활성화

### 3.6 실행 제어 및 진단
| 옵션 | 설명 |
|---|---|
| `--dry-run` | 네트워크 API 및 파일 I/O 없이 생성 계획, 프롬프트, 젠잇 블록만 시뮬레이션 |
| `--mock` | WebUI 호출 없이 더미 이미지(1x1 PNG)를 실제 WebP로 변환·저장 |
| `--test` | 52개 항목 단위 검증 스위트(T1~T37e) 실행 후 종료 코드 0/1 반환 |

---

## 4. 코드 표현식 및 모드 해석 (`--mode`, `--codes`)

- **코드 표현식 판별**: 숫자, 쉼표, 하이픈, 공백으로만 구성된 경우 코드로 인식 (`looks_like_code_expr()`).
- **섹션명 판별**: 영문/언더스코어로 구성된 경우 포즈 DB의 섹션 키로 인식.
- **다중 섹션 결합**: 쉼표로 연결 시 합집합 생성 (예: `emotions,poses,event_main`).
- **우선순위**: `--codes` 인자가 존재하면 `--mode`를 완전히 오버라이드.

| 입력 예시 | 해석 결과 |
|---|---|
| `--mode all` | DB 내 전체 포즈 코드 순회 |
| `--mode emotions` | `emotions` 섹션(00~20)만 순회 |
| `--mode emotions,poses` | `emotions` 및 `poses`의 합집합 순회 |
| `--codes 0,5,12` | 0, 5, 12번 코드만 순회 |
| `--codes 140-145` | 140, 141, 142, 143, 144, 145번 순회 |
| `--codes 0-3,21,140` | 범위와 단일값의 합집합 순회 |

---

## 5. 데이터 스펙

### 5.1 캐릭터 JSON 스펙 (`characters/{name}.json`)

```json
{
  "prefix": "ren",
  "default_mode": "otokonoko",
  "positive": "masterpiece, best quality, ... BREAK 1boy, solo, androgynous, (otokonoko:1.2), ...",
  "negative": "worst quality, low quality, ..., 1girl, female, (multiple views:1.5)",
  "ref_weight": 0.7,
  "background": "tropical beach bar terrace background, ocean view through window",
  "seed": 1234567890,
  "width": 832,
  "height": 1216
}
```

- **`default_mode` 자동 병합 규칙**:
  - `female` → `emotions,poses,h_scenes` (코드 00~75)
  - `otokonoko` → `emotions,poses,scenes_otokonoko` (코드 00~37, 140~170)
  - **이벤트 자동 결합**: 해당 로스터에 `events.json`이 존재하고 `event_*` 섹션이 정의되어 있으면, `default_mode` 해석 시 자동으로 `event_*` 섹션들을 뒤에 병합함 (`emotions,poses,scenes_otokonoko,event_main,event_random`).
- **`positive` 방식**: `BREAK` 문법을 포함하여 공통 품질 티어와 캐릭터 본체를 명확히 분리.
- **`background`**: 해당 캐릭터에 고유 배경이 필요한 경우 명시.

### 5.2 포즈 데이터베이스 스펙 (`pose_database.json`)

```json
{
  "_schema": { "version": "2.0" },
  "_profiles": {
    "female": {
      "base_positive": "masterpiece, best quality, ..., 1girl, solo",
      "base_negative": "worst quality, ..., 1boy, male, masculine"
    },
    "male_otokonoko": {
      "base_positive": "masterpiece, best quality, ..., 1boy, androgynous, feminine face",
      "base_negative": "worst quality, ..., muscular, manly, beard"
    }
  },
  "emotions": {
    "00": {
      "label": "평상",
      "prompt": "standing, neutral face, looking at viewer"
    },
    "01": {
      "label": "미소",
      "prompt": "standing, gentle smile, looking at viewer"
    }
  },
  "poses": {
    "21": {
      "label": "뺨쓰다듬기",
      "prompt": "pov, reaching hand toward viewer, standing"
    }
  },
  "scenes_otokonoko": {
    "140": {
      "label": "항문선교",
      "prompt": "lying on back, legs spread, missionary position, (black censor bar:1.25)"
    }
  }
}
```

- `_`로 시작하는 최상위 키는 포즈 순회 대상에서 제외됨.
- 포즈 엔트리는 딕셔너리(`{"label": "...", "prompt": "..."}`) 및 단순 문자열(`"..."`) 형식을 모두 지원함.
- `label` 필드가 존재할 경우 콘솔 진행 상태 및 젠잇 마크다운 상태 매핑 가이드의 **라벨(이름표)**로 우선 채택되며, 부재 시 프롬프트의 **첫 태그(쉼표 이전)**가 폴백 라벨로 추출됨.

### 5.3 프로젝트 전용 이벤트 스펙 (`events.json`)

```json
{
  "event_main": {
    "201": {
      "label": "칵테일건네기",
      "prompt": "standing, holding cocktail glass, serving drink"
    },
    "202": {
      "label": "비내리는바",
      "prompt": "sitting on wooden bar table, teasing expression"
    }
  },
  "event_random": {
    "211": {
      "label": "얼음넘어짐",
      "prompt": "falling forward toward viewer, accidental contact, blushing"
    }
  }
}
```

- 로스터 디렉터리 내에 위치하며, 공용 DB를 수정하지 않고 작품 전용 200번대 이벤트 씬을 주입.
- `load_pose_db()` 호출 시 공용 DB와 인메모리에서 안전하게 병합됨.
- 이벤트 엔트리 역시 `label` 필드를 통해 한글 라벨을 명시할 수 있음.

### 5.4 프로젝트 전용 배경 스펙 (`background.json`)

```json
{
  "default": "tropical beach bar terrace background, ocean view through window, afternoon sunlight",
  "night": "dimly lit beach bar interior, warm lamp lighting, dark night ocean outside window"
}
```

- 감정(`emotions`, 00~20) 씬에 한하여 `clean background` 태그를 해당 배경 프롬프트로 치환.
- 침대, 샤워부스, 야외 등 고유 환경이 명시된 포즈 및 H씬에는 절대 침범하지 않음.

---

## 6. 프롬프트 조립 메커니즘 (`assemble_prompt`)

### 6.1 포지티브 조립 규칙 (BREAK 전진 배치)

`positive`에 `BREAK` 키워드가 존재하는 경우:
```
{품질 티어}, {포즈 DB 태그} BREAK {캐릭터 외형}, {prefix}_{코드}
```
1. **1번 청크 (품질 + 포즈/구도)**: CLIP 최우선 attention 확보, 카메라 앵글 및 포즈 반영률 극대화.
2. **2번 청크 (캐릭터 본체/외형)**: 의상/헤어 색상이 포즈나 배경으로 번지는 Color Bleed 차단.
3. **배경 치환**: 감정 씬(00~20)의 경우 포즈 DB 내 `clean background` 태그가 해석된 배경 프롬프트로 동적 치환됨.

### 6.2 네거티브 조립 규칙
- 캐릭터 JSON의 `negative` 필드가 최우선 사용됨.
- 캐릭터 JSON의 `negative`가 없으면 `profile.base_negative` + `custom_neg`로 결합.

---

## 7. WebUI 통신 및 페이로드 규격

### 7.1 기본 txt2img 페이로드
```json
{
  "prompt": "...",
  "negative_prompt": "...",
  "steps": 24,
  "cfg_scale": 6.0,
  "width": 832,
  "height": 1216,
  "sampler_name": "DPM++ 2M SDE Karras",
  "seed": -1,
  "alwayson_scripts": {}
}
```

### 7.2 ControlNet (IP-Adapter) 주입 규격
```json
{
  "alwayson_scripts": {
    "controlnet": {
      "args": [{
        "enabled": true,
        "input_image": "<base64_encoded_image>",
        "module": "ip-adapter_clip_sdxl",
        "model": "ip-adapter_xl [hash]",
        "weight": 0.7,
        "resize_mode": "Crop and Resize",
        "control_mode": "Balanced",
        "pixel_perfect": true
      }]
    }
  }
}
```

### 7.3 Graceful Fallback 에러 복구
- WebUI API 호출 시 **422 Unprocessable Entity** 또는 **500 Internal Server Error**가 발생할 경우:
  1. `[WARN]` 경고 출력.
  2. FreeU, ADetailer 등 부가 확장을 비활성화한 순정 페이로드로 즉시 자동 재시도.
  3. ControlNet 유닛은 유지하여 캐릭터 일관성 보존.
- **404 Not Found** 또는 **ConnectionError** 발생 시에는 WebUI 미실행 상태로 판단하여 즉시 중단 (`ConfigError`).

---

## 8. 저장소 및 파일 I/O 불변식

- **저장 경로**: `projects/{roster}/assets/{prefix}/{prefix}_{코드자릿수}.webp`
- **파일명 패딩 폭**: `max(2, 전체 대상 최대 코드의 자릿수)` (100번대 이상 존재 시 폭 3으로 통일).
- **원자적 저장**: `.part` 임시 파일에 먼저 기록한 후 `os.replace()`를 통해 원자적으로 교체. 생성 도중 프로세스가 강제 종료되어도 0KB 손상 파일 방지.
- **중복 스킵 및 재개**: 대상 파일이 이미 디스크에 유효하게 존재하면 API 호출 없이 자동으로 스킵. 강제 재생성 시 해당 `.webp` 파일만 삭제 후 재실행.

---

## 9. 종료 코드 (Exit Codes)

| 코드 | 상태 | 발생 조건 |
|---|---|---|
| `0` | 정상 완료 | 모든 대상 에셋 생성 완료, `--test` 전항목 PASS |
| `1` | 런타임 오류 | `ConfigError` 발생, WebUI 연결 실패, `--test` 검사 실패 |
| `2` | 인자 오류 | argparse 필수 파라미터 누락 |
| `130` | 사용자 취소 | 키보드 인터럽트 (Ctrl+C) 수신 |

---

## 10. 자체 진단 테스트 스위트 (T1~T37e, 총 52개)

`python sd_batch_generator.py --test` 명령으로 단독 실행:
- **T1~T7 [데이터 검사]**: 파일 실존, JSON 문법, 최상위 딕셔너리, 비정수 키 차단, 빈 프롬프트 방지, 중복 코드 검출, 유효 엔트리 수량.
- **T8~T17 [로직 검사]**: 정수 정렬, 패딩 폭 계산, 코드 표현식 파싱, 파일명 조립, 마크다운 라인 일치, `PoseEntry.label` 명시적 라벨 및 첫 태그 폴백(T13b), prefix 화이트리스트 검증, 태그 정규화 및 충돌 감지.
- **T18~T20 [이벤트 및 프롬프트]**: `events.json` 동적 병합, `default_mode` 이벤트 섹션 자동 확장, `assemble_prompt` BREAK 전진 배치.
- **T21~T26c [참조 및 페이로드]**: 확장자 우선순위(.png>.jpg>.jpeg>.webp), base64 왕복 검증, ControlNet 유닛 필수 키, 페이로드 불변성.
- **T27~T32b [검증 및 통계]**: ref_weight 범위 허용치, 모델명 매칭, 시간 집계, VRAM 메모리 파싱.
- **T33~T35 [캐릭터 및 안전성]**: `apply_character_to_args` 옵션 오버라이드 우선순위, 전체 로스터 캐릭터 JSON 무결성(46개 파일), 전체 로스터 캐릭터 금지 태그(sweat/liquid) 완전 배제 검증(T34b), 감정 씬 배경 치환 vs 침대 씬 보존.
- **T37a~T37e [배경 시스템]**: CLI 명시 우선순위, 파일 부재 시 None, default 배경 로드, night 배경 로드, 미등록 프리셋의 default 폴백.
